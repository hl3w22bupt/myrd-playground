# concept-pool-v1.md — N1 征集窗口收口黑板（A 线）

> 更新时间：2026-09-20（12:00 收口批次）
> 负责人：主策划整合（A1 策划 / A2 程序 / A3 美术 / A4 QA 各节署名）
> 下一步：9/21 呈主人拍板 §A5 材料包（含 §A2.3 spike 桌面实证结论与追认代记确认项）

---

## §0 输入完整性声明（整合人必读）

**本窗口的原始四卡征集文案不在本工作区**（探查记录：无 N1 历史黑板 / 无概念卡存档 / 无 R1–R6 红线既有定义）。
因此本文件如实执行：

1. **snake-ghost（残影）卡**：任务书点名卡，信息充分 → 走完 A1→A4 全流程（§A1–§A4）。文案为
   **收口示范稿**，标注「原稿缺失，进终裁前需原稿作者核对」。
2. **其余三卡**：原始文案缺失 → **不伪造征集史、不现场代写新概念**（A5 红线：材料包不现场新增论据）。
   三卡各留回填模板（§A1.3 / §A2.4 / §A3.3 / §A4.3），原稿回传后 30 分钟内可齐套。
3. **R1–R6 红线**为本次收口固化版（§0.1），系从任务书验收口径逐条反推；如与主人既有口径冲突，
   以主人口径为准，已复检结论自动重审。

### §0.1 R1–R6 红线（本窗口固化版）

| # | 红线 | 判定口径 |
|---|---|---|
| R1 | 无形容词 | 禁「好玩/爽/耐玩/流畅」等不可判定评价词，逐句可核对 |
| R2 | 三字段句式完整 | core_loop 含单循环时长上限+反馈延迟上限；aha_moment 含触发事件+可观测代理+首触时限；retention_hook 含量化门槛 |
| R3 | 首发平台行明确 | 平台 + 最低版本/适配口径一行写死 |
| R4 | 性能主张须实证 | 任何「能/不能」结论绑定录屏/帧率/内存数据路径，无实证不得进终裁 |
| R5 | 表现参数可执行 | 视觉/音频规格到可开工粒度（帧数/透明度梯度/颜色偏移等），附注形式不另起文档 |
| R6 | 成本项显式 | 新风格线/新系统引入的成本必须标明，不隐含 |

---

## §A1 游戏策划 · 句式化改写（12:00 批次）

### §A1.1 snake-ghost（残影）卡 · 收口示范稿

> 来源声明：原稿缺失，本稿按 R1/R2 句式标准重写，**进终裁前需原稿作者核对**。

- **core_loop**：玩家操控蛇在网格吃食成长；蛇身每格移动在身后留驻残影，残影存活 3.0s；
  玩家穿越存活期内自身残影触发回溯得分（残影越长回溯分越高）。单循环（吃食→留影→回溯或躲避）
  时长上限 **8s**；输入到转向可见的反馈延迟上限 **100ms**（超过即判反馈缺陷）。
- **aha_moment**：触发事件 = 首次穿越 3.0s 内自身残影并完成回溯连击（≥2 连）；
  可观测代理 = 首局内「回溯连击 ≥2」触发次数 ≥1（遥测可统计）；首触时限 = 首局 **60s** 内。
- **retention_hook**：每日首局完成回溯连击 ≥3 次解锁当日「加速种子」（次日开局速度档 +1）；
  量化门槛 = D1 回访玩家中「当日完成 ≥3 次连击」占比 **≥40%**，低于门槛则回溯分与残影时长进 spec 期调参。
- **首发平台行**：移动 Web（iOS Safari ≥15 / Android Chrome ≥110），竖屏单手竖持可玩。
- **残影参数锁**：残影持续 **3.0s**（锁值）。原 5s 口径作废；5s 仅作为 spec 期调参区间上界保留
  （允许区间 [2.5, 5.0]s，出窗须重走修订产生新版本）。本卡无形容词（R1 ✅）。

### §A1.2 参数锁快查

| 参数 | 锁值 | spec 期调参区间 | 出窗动作 |
|---|---|---|---|
| 残影持续 | 3.0s | [2.5, 5.0]s | 重走 `POST /:id/revisions` version+1 |

### §A1.3 其余三卡 · 改写模板（原稿回传后套用）

```
- core_loop：<玩法一句话>；单循环时长上限 __s；反馈延迟上限 __ms
- aha_moment：触发事件 = __；可观测代理 = __（遥测可统计）；首触时限 = 首局 __s 内
- retention_hook：<机制一句话>；量化门槛 = __（可统计、可判定）
- 首发平台行：__
- 参数锁：<核心参数> __（锁值 + 调参区间 + 出窗动作）
```

---

## §A2 游戏程序 · spike 与三行式批注（12:00 批次）

### §A2.1 snake-ghost 残影独立 spike 方案（不进主线分支）

- 分支：`spike/snake-ghost-afterimage`（独立分支，禁止合入主线）。
- 实现口径：Godot 4.3，残影节点池（Polygon2D 色块，同 A3 规格 §A3.1），逻辑 10Hz 存档点采样。
- 采样：`Engine.get_frames_per_second()` 逐秒记录；`OS.get_memory_info()` 的 stack/heap 项起止差值；
  录屏 iOS Safari（真机）+ 桌面 Chrome 各 ≥60s。
- 落盘：`games/game/qa/snake-ghost-spike/`（录屏文件 + fps.csv + mem.json + run.md）。
- **下午批次进展（游戏程序，2026-09-20）**：spike 资产一键化落盘——`spike_main.gd`（§A3.1 全规格：
  10Hz 存档点采样 / 3.0s 存活 / 30 节点池 / α 0.35→0 线性 / H−24° V+10%）+ `spike_main.tscn` +
  `run-spike.sh`（在临时独立工程内运行，零主线侵入、不碰 project.godot 与主场景）+ `README.md`
  （桌面无头机判 / 桌面窗口录屏 / iOS Safari 真机三套运行方式 + §A2.2 判据 + 回填纪律）。
  有 shell 的执行者一条命令出数据：`bash games/game/qa/snake-ghost-spike/run-spike.sh`。

### §A2.2 「能/不能」实证判据（写死， Spike 只回答这两个词+数据）

| 判据 | 「能」阈值 | 「不能」处置 |
|---|---|---|
| 移动端帧率 | 60s 内 ≥95% 采样点 60fps，1% 低帧 ≥45fps | 降档重测（残影采样 10Hz→5Hz）；再不达 = 不能 |
| 内存 | 60s 残影满载堆增量 <50MB | 同上降档重测 |
| 反馈延迟 | 输入→转向可见 ≤100ms（录屏逐帧核对） | 不能 |

### §A2.3 结论（2026-09-21 实证批次 · 游戏 QA 与主策划联署）

**桌面口径 = 能**（实跑证据已落窗内）：

```
SPIKE: verdict_desktop fps_pct_at60=98.3%(阈值≥95%) p1_low_fps=58.5(阈值≥45) heap_delta=0.0MB(阈值<50MB) pool_exhausted=19
SPIKE: verdict_desktop fps=能 mem=能 => 桌面口径 能
```

- 数据落盘：`games/game/qa/snake-ghost-spike/data/20260921-010252/`（fps.csv 逐秒采样 / mem.json / run-headless.log 原文）。
- 判据对照（§A2.2）：帧率 ✅（98.3% ≥95%，1% 低帧 58.5 ≥45）；内存 ✅（0.0MB <50MB）；pool_exhausted=19 记录在案
  （30 节点池在满速场景耗尽 19 次，属池回收节奏观测项，不入门槛）。
- **剩余缺口**：iOS Safari 真机录屏项（README §2③）需真机，本窗口内无真机 → 未补。按 §A2.2 判据表，
  snake-ghost 当前为「**桌面过、移动端待证**」——首发平台为移动 Web，严格判仍差真机一项。
- 修复记录：spike 资产两处缺陷已修并实跑验证（spike_main.gd `Color.get_h/get_s/get_v` → Godot 4 `.h/.s/.v`；
  run-spike.sh heredoc 生成 project.godot 的 `orientation` 丢引号致 error 43 + `$OUT` 未花括号被 bash 吞解析）。
- 处置：桌面证据已在 24:00 窗内落盘，「淘汰不保卡」默认分支被桌面证据顶起；**是否以「桌面过 + 移动端待证」
  进终裁，归主人裁决（9/21 材料包 §A5.4 第 2 项一并呈批）。**

### §A2.4 其余三卡 · 三行式批注模板（原稿回传后套用）

```
- 性能预算：<卡面主张的资源类型>，预算 __（帧率/内存/包体），实证路径 __
- 平台兼容：首发平台 __，风险点 __（API/浏览器版本），降级方案 __
- 实现风险：最高风险 __，影响面 __，spike 需求 __（要/不要，理由一行）
```

---

## §A3 游戏美术 · 迷你风格参考卡 + 残影视觉规格（12:00 批次，附注形式）

### §A3.1 残影视觉规格（可开工粒度）

- **帧数**：逻辑 10Hz 存档点采样（每 0.1s 一个残影节点），非逐帧拖尾；3.0s 满载上限 30 存活节点。
- **透明度衰减梯度**：线性 α 0.35 → 0.00 按存活时间归一化（t=0s:0.35 → t=3.0s:0.00，无台阶无缓动）。
- **颜色偏移**：色相 H −24°、明度 +10%（「过去时」冷读法），不引入新色板；本体色不变。
- **同帧规则**：残影节点在存档帧与逻辑结算同帧入队（对齐 B 线音画同 tick 口径）。

### §A3.1a 规格状态附注（游戏美术 · spike 证据响应，2026-09-21 凌晨批次）

> 证据路径：`games/game/qa/snake-ghost-spike/data/20260921-010743/`（fps.csv + mem.json + run-headless.log 三件套）。verdict 行原文：`fps_pct_at60=98.3%(阈值≥95%) p1_low_fps=58.5(阈值≥45) heap_delta=0.0MB(阈值<50MB) => 桌面口径 能`。另两组目录（20260921-005621 / 010252）无 verdict 行，判过程跑不采信。§A2.3/B-4/run.md 回填为跑者落点，美术不代出综合结论。

- **桌面指标：通过** → §A3.1 全规格（10Hz 采样 / 3.0s 存活 / α 0.35→0 线性 / H−24° / V+10%）在桌面口径下**维持可开工，随卡进终裁**。
- **完整性挑明（不推翻 verdict，只圈条件）**：
  1. **移动端两项无数据**（Safari 真机帧率、反馈延迟 ≤100ms）——README §3 明文桌面数据不能替代；本批证据实证覆盖 §A2.2 三判据中的内存 1 项 + 帧率的桌面口径。规格状态 = **桌面条件冻结**，完整「能」待真机补核后成立。
  2. **`pool_exhausted=19`**：3.0s×10Hz=30 为临界池容，时钟抖动致 60s 丢 19 个采样点（≈3.2%，表现为残影链偶发 1 格缺口）。**美术规格侧认领**：系本规格「30 节点池」条款的边界缺陷而非实现缺陷；修订建议 = 池容 +1（31），随 spec 期调参窗在 [2.5,5.0]s 区间重验，不改锁值 3.0s。
  3. **色板轮换未生效**：日志 `second` 列恒 0（`_seconds_done` 未递增）→ 60s 全程仅 1 号色，「5 色板全覆盖」视觉验证未完成；不影响 verdict（渲染成本与色值无关），真机录屏核对时顺带覆盖。

### §A3.2 snake-ghost 迷你风格参考卡（≤10 行，四要素各一行 + 风格锚）

> 下午批次（游戏美术）补全：初版缺「光照/比例」两要素明示行，现按工程实况（candy.gd 假光照参数 / CELL=96 半径 38）补齐；四要素各一行 + 场地/反馈并入，共 7 行。

```
卡：snake-ghost 残影（参考卡，非素材交付）
- 调色板：本体复用现行糖果色板 5 色；残影同色 H−24°/V+10% 偏移；深紫底 Color(0.13,0.11,0.24,0.88)；不加新色不入新色板
- 光照：糖果本体=程序化假光照（亮面白 30%/暗面 45%/高光/投影 7px，candy.gd 现行参数）；残影=平面色块直出（无 facet 无投影，「过去时」冷读法）
- 线条：几何色块、无描边线条语言；格子凹槽内嵌黑 22% + 白 5% 描边（同现行棋盘 board.gd）；回溯金环 FX_COLOR_GOLD
- 比例：格 96px、糖果外接半径 38（每边留 10px 缝隙）；残影与本体同格同尺寸同形，仅透明度区分
- 反馈：回溯触发金环 FX + 飘分文字复用 FX_COLOR_GOLD 同规格；残影衰减 α 0.35→0 线性（规格见 §A3.1）
- 风格锚：极简几何色块 + 金色反馈（与 assets.md §0 风格卡同源，零新风格资产）
```

### §A3.3 成本项标注（R6）

- 新风格线成本：蛇体拖尾渲染层 + 30 节点对象池（新增，非复用）；内存与帧率成本待 §A2 spike 实证，
  实证前**不计入**已承诺预算。规避项：无贴图、无新色板、无新字体（延续现行风格线）。
- 其余三卡参考卡：原稿缺失，无法产出（模板位已留）。

---

## §A4 游戏 QA · R1–R6 红线复检（12:00 批次）

### §A4.1 snake-ghost 卡复检单

| 红线 | 判定 | 依据 / 缺陷句位置 |
|---|---|---|
| R1 无形容词 | ✅ 过 | §A1.1 全文无「好玩/爽/耐玩」类词 |
| R2 三字段句式 | ✅ 过 | core_loop（8s/100ms）、aha_moment（触发事件+代理≥1 次/60s）、retention_hook（≥40%）量化齐全 |
| R3 首发平台行 | ✅ 过 | §A1.1 平台行（iOS Safari ≥15 / Android Chrome ≥110，竖屏单手） |
| R4 性能主张实证 | ⚠️ 条件过 | 卡面无未实证「能」主张（合规）；残影可行性挂 §A2.3 PENDING_EVIDENCE —— spike 证据未落则整卡不进终裁 |
| R5 表现参数 | ✅ 过 | §A3.1 到可开工粒度 |
| R6 成本项 | ✅ 过 | §A3.3 显式标注 |

**结论：可验收（带 R4 条件）** —— 条件即主策划写死口径：24:00 前证据落黑板 → 进终裁；未落 → 淘汰不保卡。

### §A4.2 「缺一卡不进材料包」执行口径

三卡原稿缺失 → 三卡**不进**材料包（无文案即无复检对象，QA 不做无输入判定）。
材料包按「单卡 + 条件」整合，缺口升级（§A5）。

### §A4.3 三卡复检表模板（原稿回传后套用）

```
| 红线 | 判定 | 缺陷句位置 |
| R1..R6 | ✅/❌ | 逐条引用原句 |
结论：可验收 / 需改写（改写点：__）
```

---

## §A5 主策划 · N1 终裁材料包（13:00 批次）

> 整合声明：本节只汇总上文四路产出与既定口径，**未新增任何论据**（A5 验收信号）。

### 1. 复检结论汇总

| 卡 | 状态 | 进终裁？ |
|---|---|---|
| snake-ghost（残影） | 可验收（带 R4 条件：spike 证据 24:00 前落黑板） | 条件进 |
| 卡 2/3/4 | 原稿缺失，未复检 | 不进 |

### 2. 淘汰判据（主策划写死口径，逐字执行）

- snake-ghost：spike 证据（§A2.1 落盘物）2026-09-20 24:00 未落黑板 → **淘汰不保卡**；
  今晚窗口关闭前补齐仍可加验；过窗彻底出局。
- 三卡：原稿 9/21 呈批前仍未回传 → 不复活、不代写；按既有口径转 **N2 首日开 48h 补位窗**。

### 3. 成本项汇总

- snake-ghost：拖尾渲染层 + 30 节点池（新风格线成本，待 spike 实证）；无贴图/新色板/字体成本。
- 窗口成本：三卡缺失导致本窗口实际收口为「单卡 + 条件」，N2 需补位窗（48h）。

### 4. 建议终裁顺序（9/21 呈主人）

1. **先裁窗口口径**：三卡原稿缺失 → 「N1 按单卡收口」还是「窗口延期等原稿」（需主人裁决，见 blockers.md B-2 升级项）。
2. snake-ghost：看 24:00 前证据状态 → 落了先裁它（唯一成卡）；没落按 §2 淘汰，N1 本轮 0 卡，窗口收口转 N2。
3. 三卡原稿回传后 → 走同一 A1→A4 流程 → 48h 补位窗内补裁。

### 5. 人工验收声明

以上全部为**呈批材料**。终裁（approve/否决）只能由主人作出；机器与职能成员不替人判断「好不好玩」。
