# m2-kickoff-review.md — M2 立项包 QA 复审（每条指标带可核对复验方式）

> 更新时间：2026-09-22（M2 立项冲刺 · 游戏 QA 复审，主策划整合落黑板）
> 负责人：游戏 QA（复审出具）/ 主策划（整合）
> 复审对象：v1.3 草案（design-spec-pixel-fives-v1.3-draft.json）+ 提案（v1.3-m2-draft-proposal.md）
>           + 美术清单（asset-list-m2-feedback-v1.md）+ 程序批注（feasibility-m2-feedback-v1.md）
> 复审结论：**通过送审（4 项备注，0 打回）**——主人批准 v1.3 前不得动码落资产

## 一、逐条指标复验方式核对（QA 主责）

| # | 指标 | 提案口径 | 可核对复验方式（QA 认可） | 判定 |
|---|---|---|---|---|
| 1 | 单局 ≤3 分钟 | duration_cap_s ≤180 不回退 | full-match 冒烟 S5（duration 90∈[80,100]）+ bot-sim duration 比率=1.0；命令已冻结可粘贴 | ✅ |
| 2 | 场均进球 ∈[2,4] | bot_curve.avg_goals_range | `node pixel-fives/tools/bot-sim.mjs --profile onboarding --seeds 42..141 --json` → 逐场 goals_total 求均值；**逐场 [1,12] 同时断言（不回退）**；命令随 --profile 扩展冻结 | ✅（口径备注 ①） |
| 3 | 反馈首帧 ≤100ms | feedback_first_frame_ms | headless：输入 tick == 反馈事件排程 tick（acc-10 落点测试，exit 0/1）+ 真机抽测录屏逐帧（归真机批次台账）；**两口径都过才算核销** | ✅（备注 ②） |
| 4 | 动效 ≤300ms | anim_max_ms | acc-11 落点测试逐动效时长断言；**适用范围已正确排除 M1 已批 A06/庆祝**（避免与 v1.2 冻结值冲突） | ✅ |
| 5 | 震动 3 级 | haptic_levels=3 | acc-12 映射表断言（事件→级别）+ 真机手感复核；iOS Web 不支持 → **静默降级不报错 + 真机台账**（程序批注 §5 已对齐） | ✅（备注 ③） |
| 6 | 首局胜率 65–75% | first_match_win_rate | 本地埋点周报还原：获胜首局会话 ÷ 完成首局会话；**拒绝任何无样本期的估算值** | ✅ |
| 7 | 长期胜率 ≈50% | long_term_win_rate | 同上按周滚动；样本不足不出结论 | ✅ |
| 8 | 教学 ≤30s | tutorial_time_s | 埋点首触→首进球引导链中位数（acc-14 落点测试 headless 断言链路存在性；真机中位数归埋点周报） | ✅ |
| 9 | 首局完成率 ≥90% | first_match_complete_rate | **样本口径已写死**：安装后 48h 内新玩家首局、开始→终局达成率、自然周聚合、周样本 ≥200 方判定——不足即「本周不出结论」，QA 不接受补样估算 | ✅ |
| 10 | 埋点双口径 | dual_caliber=[platform,local] | acc-15 降级断言（平台不可用→本地缓冲，schema 逐字一致，重启不丢）+ QA 抽查缓冲文件字段对照表 | ✅ |

备注：① onboarding/ranked 两档都要跑全量 100 场，禁止抽样 20 场代替；② headless 口径与真机口径
分开记账，不允许以 headless 数值冒充真机实测；③ 震动 3 级的「级别手感」最终由主人真机体验裁决
（QA 只核映射与降级正确性）。

## 二、四件套一致性核对

| 件 | 核对项 | 判定 |
|---|---|---|
| v1.3 草案 JSON | 15 条 acceptance（v1.2 九条原样 + M2 六条 pending）；M2 numeric 10 键与提案 §2 表逐条对上 | ✅ |
| 策划提案 | scope 锁定三块 + 不做清单 + 9/12 预排移列 M3 的决策记录在案（§1.3） | ✅ |
| 美术清单 | 4 项动效全部 ≤300ms（125–250ms）；色值全部卡内记号零卡外；bytes 增量 27KB 有预算表 | ✅ |
| 程序批注 | 三块零 core 改动；60fps 并表有 M1 实测锚（tick p95 0.002ms）；埋点 5 事件 schema 最小化 | ✅ |
| 交叉 | 美术「首帧承诺值」↔ 程序「同 tick 排程断言」↔ 提案「两口径分开记账」三处口径一致 | ✅ |

## 三、QA 复审结论与前置条件

1. **通过送审**：指标可核对性齐备，无一条「无法复验的口号」；4 项备注随 v1.3 送审单一并呈主人。
2. **前置红线**：主人批准 v1.3 之前——零动码、零资产落盘；M1 门禁（acc-01..09）每冲刺节点复跑，
   任何一项转红即停 M2 新功能先修回归（提案 §4 已写死，QA 认可）。
3. **acc-05 追认**：今日程序当日交付 tests/ui/onboarding-hint.test.mjs 并 8/8 过（见
   gate-logs/m1-recap-20260922-pixel-fives/8-onboarding-hint.log）——QA 追认该验收项闭环；
   M2 的 acc-14 在其基础上扩展，无重叠断言。

## 变更记录

- 2026-09-22 游戏 QA：复审出具（逐条复验方式 + 四件套一致性 + 前置条件）。
- 2026-09-22 主策划（整合）：落黑板；与 verdict/提案/清单/批注四处交叉引用核对。
