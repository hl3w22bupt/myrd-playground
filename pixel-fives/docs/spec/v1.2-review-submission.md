# v1.2 送审单 · GameDesignSpec「像素街机足球 Pixel Fives」M1 段

| 项 | 值 |
|---|---|
| 收件 | **主策划**（唯一 approved 执行人） |
| 送审 | 游戏策划线 |
| 日期 | 2026-09-12 |
| 送审物 | ① 真源：`pixel-fives/docs/spec/design-spec-v1.2.md`（v1.2-DRAFT-PENDING-REVIEW，完整自洽文本）② 机器可读导出：`.myrd/spec/design-spec.json`（六段全量，2026-09-12 本轮补全） |
| 请求动作 | 按 §7 复核清单六问逐条 yes/no；全 yes ⇒ 盖章 approved（版本链 v1.1→v1.2）；任一 no ⇒ 打回策划线修订 |

---

## 1. 按裁决修订自检表（R-01..R-10 · 逐条落文核对）

策划线已将 2026-09-12 终裁与两条流程外裁决**全量落文**，每条给出真源位置 + 仓库证据，主策划可逐行核对：

| id | 裁决要点 | 真源落文（md） | 机器导出（json） | 仓库证据（本轮实测核对） |
|---|---|---|---|---|
| R-01 | A06 锁 9 帧@12fps=0.75s；contact_frame=3 仅视觉不阻塞判定 | §0 R-01、§5 `a06.*` 行 | `revision_detail[0]`、`numeric.values[18]` | `constants.js` `A06={frames:9,fps:12,duration_s:0.75,contact_frame:3}`；`assets/manifest.json` A06 同值；`player.js` kickAnimT 不参与判定 |
| R-02 | 新增 A08 `sfx-goal-hit` + 字段 `goal_sfx_at_s=0.0`（音效不删、变可测） | §0 R-02、§3.1 goal、§5 `goal_sfx_at_s`/`a08.*` 行、el-10 | `revision_detail[1]`、`numeric.values[16][19]`、`entities.list[2]` | `constants.js` `A08`+`GOAL_SFX_AT_S=0.0`；`goal.js` `sfxSchedule()`；`assets/a08-sfx-goal-hit.spec.json`（0.6s/44.1kHz/seed=20260912） |
| R-03 | 红线 `input_to_shot_latency_ms ≤ 50`（v1.3 以实测定标，v1.2 只锁红线） | §0 R-03、§5 红线行、acc-02 | `revision_detail[2]`、`numeric.values[17]`、`acceptance.items[1]` | `constants.js` `RED_LINE_INPUT_TO_SHOT_MS=50`；`player.js` tryKick 同 tick 即时出球（≤16.7ms） |
| R-04 | onboarding-hint check 落点 = UI 集成测试；bot 对局 / bot-sim 不背此断言 | §0 R-04、el-09、acc-05 | `revision_detail[3]`、`levels…el-09`、`acceptance.items[4]` | check = `tests/ui/onboarding-hint.test.mjs`（acc-05 原文标注「UI 集成测试」） |
| R-05 | out_of_scope 负面清单（冻结项，PR 出现即打回） | §1.4（7 条） | `meta.out_of_scope`（7 条） | 与 Team Lead 冻结项逐条对齐（AI女友/新游戏/多模式/Steam·Roblox/70-30/M1 禁做/16px 公度） |
| R-06 | 命名配色锁定：赤焰 Fives `#D93A2B` / 霜蓝 Fives `#2B6BD9` | §2.2 球队表 | `world.teams` | `constants.js` `TEAMS` 同值；`manifest.json` style_lock.palette `r/b/R/B/x/y` 同色板 |
| R-07 | 落点规则 `src/entities/<id>.js` / `src/levels/<id>.js`（相对 `pixel-fives/`） | §3 开头、§4.1 | `entities.placement_rule` | 在库实测：`src/entities/{player,ball,goal}.js` 存在；`src/levels/pitch.js` 为落点声明（程序线 M1 内交付） |
| R-08 | bot-sim 契约冻结：拆两比率字段；errors 非空⇒exit≠0；补 `per_game[]`+`meta`；seeds=42..141 | §0 R-08、§5.1 全节、acc-03/04 | `numeric.bot_sim_contract`（frozen:true）、`revision_detail[7]` | `constants.js` `BOT_SIM_SEEDS={start:42,end:141}`；报告 schema 六字段与程序实现接口一致 |
| R-09 | 继承声明：本版完整自洽，不依赖 v1.1 原文 | 文件头「继承」行 | `meta.inherited_from` + $note | v1.1 原文随黑板不可达；实现与验收不依赖其回溯 |
| R-10 | 采纳程序线提案：`kick_cooldown_s=0.3`、`reach_px=13`（v1.3 调参候选） | §0 R-10、§5 两行〔采纳〕 | `revision_detail[9]`、`numeric.values[8][9]` | `constants.js` `PLAYER.KICK_COOLDOWN_S=0.3`、`PLAYER.REACH=13` 同值 |

## 2. 策划线本轮自检发现并已处置的 1 处漂移

| 发现 | 处置 |
|---|---|
| 程序线 `PENDING_APPROVED_SLOTS` 登记 QA 区间两键为 `bot.goal_range` / `bot.duration_in_range_s`，spec 侧写 `bot_sim.*`，键名不一致会导致 approved 后翻转漏 2 条 | 已在 md §5 与 json `numeric.slot_sync` 双处注明**别名映射**（同值，翻转按此对应）。若主策划认为应统一键名，批准时可要求程序线改登记键或策划线出 v1.2.1 改表键——二选一，值不变 |

## 3. 一致性快照（本轮实测，非转述）

- 数值：md §5 的 25 键 ↔ json `numeric.values` 25 条 ↔ `constants.js` 常量逐值一致（60Hz、256×160、inset 6、90s、门嘴 44/深 12、球员 16/92、球 8/230/240/0.45/2、庆祝 1.2、A06 9/12/0.75/3、A08 0.6/44100/20260912、seeds 42..141、区间 [1,12]/[80,100]、onboarding 3s）。
- 几何：el-03 门嘴带 y∈(58,102) = centerY 80 ± 44/2，与 `goal.js` mouthTop/mouthBottom 实现一致；el-05/06/07 站位 (168,80)/(88,80)/(128,80) 关于中线 x=128 对称。
- 资产对齐：§3.2 表 7 行 ↔ `manifest.json` assets A01..A08 id/帧规格一致（A06 已 9 帧，A08 已入 manifest）。
- 预算：acc-08 1,572,864B 红线 = `manifest.budget.assets_total_max_bytes`，`gen-assets.mjs` 超线 exit 1（在库）。

## 4. 请求主策划执行的动作

**批准路径**（六问全 yes 时）：
1. 平台接口恢复后：`POST /api/v1/game-design-specs/:id/revisions`（detail 引用本版 R-01..R-10），旧版自动置 superseded，v1.2 盖章 approved；
2. 通知程序线翻转 `PENDING_APPROVED_SLOTS`（11 条，含 §2 别名映射 2 条），constants.js 唯一改动点；
3. 通知 QA：verdict 前置三输入之一就位，验收包以 §6 逐条为依据；
4. 通知美术：量产解锁，`assets/manifest.json` status 同步 approved。

**打回路径**（任一 no）：退回策划线，产生 v1.2.x 或 v1.3；期间程序线按 pending-approved 槽位推进不受阻，QA verdict 继续挂起。

**环境事实声明**：平台 game-design-specs 接口与黑板 `.myrd/blackboard/` 依上轮 QA/主策划/Team Lead 三方独立实测不可达；本轮策划线执行环境无 shell 能力，未重测接口，故按「落文待归位」路径送审（待归位清单见 md §8 / json `pending_homecoming`）。接口恢复后主策划的批准动作以 §4 批准路径为准。

---

**送审人**：游戏策划线 · 2026-09-12
**打回与升级**：对 R-01..R-10 任何一条有异议 = 打回；对本送审单覆盖范围外的诉求 = 升级 Team Lead 例会（9/19）。
