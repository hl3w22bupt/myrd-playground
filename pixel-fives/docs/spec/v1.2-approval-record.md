# GameDesignSpec v1.2 批准记录 & 版本链台账（主策划盖章）

| 项 | 值 |
|---|---|
| 文书性质 | 唯一 approved 执行人（主策划）复核批准记录 + 版本链台账 |
| 批准对象 | ① GameDesignSpec v1.2（M1 段，真源 `design-spec-v1.2.md`）② 风格参考卡 v1（`docs/art/style-card-v1.md`）③ A01–A08 首批资产规格（`assets/manifest.json`） |
| 批准日期 | 2026-09-12 |
| 批准人 | 主策划（lead-game-designer，唯一 approved 执行人） |
| 生效 | 即刻：程序线翻转 `PENDING_APPROVED_SLOTS`（acc-09）· QA 三份输入到齐可出 verdict · 美术量产解锁 · contract-check 切强制模式（acc-07 启用） |
| 归位备注 | 平台 `game-design-specs` 接口本轮不可达（本线无 Bash 无法 curl 实测，沿用本轮 QA/程序/Team Lead 三方实测结论），本记录为**落文待归位版盖章**；接口恢复后按 §5 归位动作补建正式版本链 |

---

## 1. spec 复核清单 verdict（v1.2-review-submission.md §7 六问）

| # | 问题 | verdict | 复核依据（实测，非转述） |
|---|---|---|---|
| 1 | §0 修订 diff R-01..R-10 与终裁逐条一致？ | **yes** | 文本（md §0）与代码双确认：`constants.js` `A06={frames:9,fps:12,duration_s:0.75,contact_frame:3}`、`A08`+`GOAL_SFX_AT_S=0.0`、`RED_LINE_INPUT_TO_SHOT_MS=50`、`TEAMS` 赤焰`#D93A2B`/霜蓝`#2B6BD9` 含暗亮阶、`BOT_SIM_SEEDS={42,141}`、`KICK_COOLDOWN_S=0.3`/`REACH=13` |
| 2 | §5 数值表批准？ | **yes** | §5 全表与 `constants.js` 逐值一致；12 个 `PENDING_APPROVED_SLOTS` 键全部可溯源到 §5（`bot.*`↔`bot_sim.*` 别名两条已注明）；R-03 红线 ≤50ms 与 60Hz 同 tick ≤16.7ms 自洽 |
| 3 | §5.1 bot-sim 报告契约与退出码规则批准？ | **yes** | 契约 frozen（拆分两比率字段、`errors` 非空⇒exit≠0、`per_game[]`+`meta`、seeds 42..141 双方同一套）；no_relax 纪律（禁止放宽区间、区间变更走 spec 新版本）符合终裁原文；QA verdict 与程序实现同源此节 |
| 4 | §4 pitch 关卡布局批准？ | **yes** | el-01..el-10 稳定 id；门嘴带 y∈(58,102) = centerY 80 ± 22 与 `GOAL.MOUTH_H=44` 一致；站位 168/88/128 轴对称；el-10 进球链含 A08@`goal_sfx_at_s=0.0` + 庆祝 1.2s 冻结；平局合法；确定性红线（禁 `Math.random`/`Date.now`，随机走 rng.js）齐备 |
| 5 | §6 acceptance 9 条及 check 落点批准？ | **yes** | 9 条均有具体 check 落点且路径已在库（tests/smoke、red-line、qa、goal-flow、contract-check、gen-assets）；acc-05 check 落点=UI 集成测试、bot 对局不背（R-04）；acc-09 明确「批准后程序线翻转并回报」 |
| 6 | 批准动作 | **执行** | 见 §2 批准动作清单与 §4 解锁矩阵 |

**结论：六问全 yes → GameDesignSpec v1.2 批准（approved）。** 依据 `reject_rule`（任一 no = 打回），无打回项。

## 2. 批准动作清单（随本记录同步执行）

1. `design-spec-v1.2.md` 文件头状态 → **v1.2-APPROVED**（§9 变更记录留痕；内容正文一字未改——内容修订必须走版本链，本次仅状态盖章）；
2. `.myrd/spec/design-spec.json` status → `approved`，补 `approved_by`/`approved_at`/`approval_record`/`version_chain`；
3. 风格参考卡 v1 → **v1-APPROVED**（美术线送审五问 verdict 见 §3）；
4. `assets/manifest.json` 顶层 status → `APPROVED`、`approved_by` 回填、8 资产 status → `approved`；
5. 本记录落库即通知：程序线翻转槽位（acc-09，翻转后回报）、QA 出 verdict（依据=spec §6 逐条）、美术启动量产序列（每批过 `gen-assets.mjs` 预算闸门）。

## 3. 美术线送审清单 verdict（evidence-art-m1.md §5 五问）

| # | 问题 | verdict |
|---|---|---|
| 1 | 风格卡 v1 四要素（20 色板/光照/线条/比例） | **yes**（20 色与 manifest `style_lock.palette` 逐色一致；换色映射 r↔b/R↔B/x↔y 三组封闭） |
| 2 | A01–A08 编号表与 id 命名（kebab-case，对齐落点规则） | **yes**（与 spec §3.2 实体-资产对齐表一一对应） |
| 3 | A06 = 9帧@12fps=0.75s、contact_frame=3 仅视觉不阻塞判定 | **yes**（卡/表/manifest/constants 四处同值；`player.js` `kickAnimT` 纯视觉注释在库） |
| 4 | A08 三层合成参数 + `goal_sfx_at_s` 默认 0.0s | **yes**（spec 字段 0.6s/44100/seed=20260912 三处同值；`goal.js sfxSchedule()` 已实现） |
| 5 | 比分数字走系统像素字体不占美术位图预算 | **yes**（el-08 scope_note 同口径） |

**结论：五问全 yes → 风格卡 v1 获批 + A01–A08 首批资产获批，量产解锁。**

## 4. 解锁矩阵（批准生效后的下游动作与验收信号）

| 下游 | 动作 | 验收信号 |
|---|---|---|
| 游戏程序 | `PENDING_APPROVED_SLOTS` 12 条逐条翻转 `pending-approved → approved`（含两条 `bot.*` 别名），constants.js 唯一改动点 | 翻转回报 + `node scripts/contract-check.mjs` mode=approved errors=0 |
| 游戏 QA | 三份输入（spec approved / P1–P4 交付 / 资产获批）已到齐 → 按 §6 逐条出 verdict | verdict 文书；bot-sim 100 场 exit 0 + 同 seed 复跑逐字节一致 |
| 游戏美术 | 量产序列：A02 方向扩展 → A06 镜像校验帧 → A01 tile 扩展 →（如改判）A07 数字位图 | 每批 `gen-assets.mjs` 合计 ≤1,572,864B |
| 主策划 | 版本链维护（§1 台账）+ M2 预排（`docs/roadmap/m2-preplan-wechat-douyin-v1.md`） | 9/19 例会复盘 M1 DoD |

## 5. 版本链台账（v1.1 → v1.2）

| 版本 | 状态 | 说明 |
|---|---|---|
| v1.1 | **superseded** | M1 段初版；原文随黑板不可达无法归档（R-09：v1.2 为完整自洽文本，实现与验收不依赖 v1.1 原文） |
| **v1.2** | **approved（本记录盖章）** | 按终裁修订 R-01..R-10；真源 `design-spec-v1.2.md` + 机器可读导出 `.myrd/spec/design-spec.json` |

**版本链纪律重申**：本批准为状态盖章，不产生 v1.2.x；此后任何内容修订（含 QA 判读所需真实墙钟、红线实测定标、区间调整）一律走新版本（v1.2.x / v1.3），禁止覆盖 v1.2；**approved 版本任意时刻至多一个**。

### 待归位动作（接口恢复后，本记录不因归位失效）

1. `POST /api/v1/game-design-specs/:id/revisions` 以 v1.2 全文（detail 引用 R-01..R-10）建正式版本，平台侧旧版自动置 superseded；
2. 以接口为准回写 `.myrd/spec/design-spec.json`；
3. 本记录 + spec + 风格卡迁入/挂链 `.myrd/blackboard/`（黑板本轮已由主策划在工作区初始化，见 blockers.md）；
4. 红线 `input_to_shot_latency_ms ≤ 50` 的实测定标进 v1.3（QA bot-sim 实跑后提修订）。

## 6. 审批留痕

- 复核方式：本线亲测——通读 spec 真源与机器导出全文、逐值核对 `constants.js`/`manifest.json`、抽查 `player.js`/`goal.js`/`world.js` 行为契约、通读三条线证据文档（prog/art + 策划送审单）；
- 未实跑项：四组 node 测试与 bot-sim 100 场**未实跑**（全轮各线运行时均无 Bash，程序线证据 §0/§8 已声明），该事实不阻塞本次**spec 批准**，但 M1 DoD 判定（9/19 复盘）必须以实跑门禁结果为准——机器不替人判断「好不好玩」，人工验收始终是最终裁决；
- 本记录作者：主策划（唯一 approved 执行人）· 2026-09-12。
