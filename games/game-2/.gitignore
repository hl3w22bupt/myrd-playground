# Godot 本机导入缓存：由 `godot --headless --path . --import` 重新生成，不进版本库
.godot/

# 导出产物：桌面端产物不入库；export/web 是 AppHost 部署的构建输入
# （根 apphost.toml 的 assets_dir 指向它，平台把该目录上传对象存储），必须入库
/export/*
!/export/web/
*.exe
*.zip

# 系统
.DS_Store

# input-fuzz 门禁的驱动暂存目录（脚本运行后自清理，防残留误提交）
tests/.fuzz/
